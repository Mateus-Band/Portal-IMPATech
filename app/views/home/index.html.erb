<% content_for :title, "Início - Portal IMPA Tech" %>

<div class="max-w-4xl mx-auto text-center mt-12">
  <h1 class="text-3xl font-bold text-blue-700">
    Portal do Estudante IMPA Tech
  </h1>
  <p class="text-gray-600 dark:text-gray-300 mt-2">
    Protótipo funcional — layout básico já configurado.
  </p>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-10">
    <% @mock_projects.each do |project| %>
      <%= render "shared/project_card", project: project %>
    <% end %>
  </div>
</div>